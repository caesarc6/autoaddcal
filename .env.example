# Server
PORT=3000
BASE_URL=http://localhost:3000
SESSION_SECRET=change-me-to-a-long-random-string

# Encryption key for stored tokens (32-byte hex = 64 chars)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=

# Supabase (Project Settings → API)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# Cron (cron-job.org) — random secret; send as Authorization: Bearer header
CRON_SECRET=

# Vercel only — helps @sparticuz/chromium load (Project Settings → Environment Variables)
# AWS_LAMBDA_JS_RUNTIME=nodejs22.x

# Google Calendar OAuth
# Create at https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3000/auth/google/callback

# Thursday auto-sync: 4 checks until WPS posts the new week (comma-separated cron, Eastern)
# Local dev only — production uses cron-job.org (see README)
THURSDAY_SYNC_CRONS=0 12 * * 4,0 15 * * 4,0 18 * * 4,0 21 * * 4
SYNC_TIMEZONE=America/New_York
